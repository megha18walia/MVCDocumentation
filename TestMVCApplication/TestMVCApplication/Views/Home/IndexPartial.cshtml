@model TestMVCApplication.Models.Movie

<tr>
    <td> @Html.DisplayFor(m => m.Movie1)</td>
    <td>@Html.DisplayFor(md => md.Genere)</td>
    <td>@Html.DisplayFor(md => md.ReleaseDate)</td>
    <td>@Html.DisplayFor(md => md.Price)</td>
    <td>@Html.ActionLink("Edit", "Edit", new { id = @Model.Id })</td>
    <td>@Html.ActionLink("Delete", "Delete", new { id = @Model.Id })</td>
    <td>@Html.ActionLink("Detail","Detail",  new { id = @Model.Id })</td>
</tr>
